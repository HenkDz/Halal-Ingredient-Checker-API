# Production-only requirements (no test deps)
# Docker image uses this instead of requirements.txt
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.9.0
httpx==0.27.0
cachetools==5.5.0
gunicorn>=22.0.0
structlog==24.4.0
prometheus-client==0.21.0
sentry-sdk[fastapi]==2.15.0
