# Production-only requirements (no test deps)
# Docker image uses this instead of requirements.txt
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.9.0
httpx==0.27.0
cachetools==5.5.0
stripe==10.8.0
gunicorn>=22.0.0
